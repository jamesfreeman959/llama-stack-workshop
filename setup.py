from setuptools import find_packages, setup

if __name__ == "__main__":
    setup(
        name="llama_stack_workshop",
        version="0.0.1",
        packages=find_packages(),
        include_package_data=True,
    )
